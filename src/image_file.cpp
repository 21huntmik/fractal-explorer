#include <iostream>
#include "image_menu.h"

int main() {
	//this function should call assignment3, passing in std::cin and std::cout as the input and output streams. this function should return what assignment3 returns
	int returnMe = assignment3(std::cin, std::cout);
	return returnMe;
} 
